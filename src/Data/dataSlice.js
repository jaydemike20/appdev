import { createSlice } from "@reduxjs/toolkit";

const productAdd = createSlice({
    name: "product",
    initialState: {
        newProduct: [],
        
    }, 
    reducers: {
       
    },
});


export const { } = productAdd.actions;

export default productAdd.reducer;

